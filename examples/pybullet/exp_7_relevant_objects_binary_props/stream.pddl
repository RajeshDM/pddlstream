(define (stream pr2-tamp)
  ;(:stream sample-pose
  ;  :inputs (?o ?r)
  ;  :domain (Stackable ?o ?r)
  ;  :outputs (?p)
  ;  :certified (and (Pose ?o ?p) (Supported ?o ?p ?r))
  ;)
  ;(:stream sample-pose
  ;  :inputs (?o ?p1)
  ;  :domain (and (Pose ?o ?p1) (Pickable ?o))
  ;  :outputs (?p2)
  ;  :certified  (Pose ?o ?p2)
  ;  ;:certified  ()
  ;)
  ;(:stream certify-movable-position-with-pose
  ;  :inputs (?o ?p ?q)
  ;  :domain (MovableConf ?o ?q)
  ;  :outputs ()
  ;  :certified (NoAgentObjectCollision ?o ?p ?q)
  ; )
  ;(:stream sample-grasp
  ;  :inputs (?o)
  ;  :domain (Graspable ?o)
  ;  :outputs (?g)
  ;  :certified (Grasp ?o ?g)
  ;)
  (:stream sample-openable-point-near-object
    :inputs (?o ?p)
    :domain (and (Pose ?o ?p) (Openable ?o))
    :outputs (?q)
    :certified (and (BConf ?q) (OpenableConf ?o ?q))
    ;:certified (BConf ?q)
  )
  (:stream sample-pickable-point-near-object
    :inputs (?o ?p)
    :domain (and (Pose ?o ?p) (Pickable ?o))
    :outputs (?q)
    :certified (and (BConf ?q) (PickableConf ?q ?p))
    ;:certified (BConf ?q)
  )
  (:stream sample-droppable-point-near-object
    :inputs (?o ?p)
    ;:domain (AtPose ?o ?p)
    :domain (and (Pose ?o ?p) (Pickable ?o))
    :outputs (?q)
    :certified (and (BConf ?q) (DroppableConf ?o ?q ?p))
    ;:certified (BConf ?q)
  )
  ;(:stream test-cfree-pose
  ;  :inputs (?o1 ?p1 ?o2 ?p2)
  ;  :domain (and (Pose ?o1 ?p1) (Pose ?o2 ?p2))
  ;  :certified (CFree ?p1 ?p2)
  ;)
  (:stream test-cfree-object-pose
    :inputs (?o1 ?p1 ?o2 ?p2)
    :domain (and (Pose ?o1 ?p1) (Pose ?o2 ?p2))
    ;:domain (Pose ?o1 ?p1)
    ;:domain (Pose ?o2 ?p2)
    :certified (CFreeObjectPose ?o1 ?p1 ?o2 ?p2)
  )

  ;(:stream inverse-kinematics
  ;  :inputs (?a ?o ?p ?g)
  ;  :domain (and (Controllable ?a) (Pose ?o ?p) (Grasp ?o ?g))
  ;  :outputs (?q ?t)
  ;  :certified (and (BConf ?q) (ATraj ?t) (Kin ?a ?o ?p ?g ?q ?t))
  ;)
  (:stream plan-base-motion
    :inputs (?q1 ?q2)
    :domain (and (BConf ?q1) (BConf ?q2))
    :outputs (?t)
    ;:certified (and (BTraj ?t) (BaseMotion ?q1 ?t ?q2))
    :certified (and (BTraj ?t) (BaseMotion ?q1 ?t) (BaseMotion ?q2 ?t))
  )
  ;(:stream plan-base-motion
  ;  :inputs (?q1 ?q2)
  ;  :domain (and (BConf ?q1) (BConf ?q2))
  ;  :outputs (?t)
  ;  :certified (and (BTraj ?t) (BaseMotion ?t1 ?q1) (BaseMotion ?t1 ?q2))
  ;)
  (:stream test-cfree-traj-pose
    :inputs (?t ?p)
    :domain (and (BTraj ?t))
    :certified (CFreeTrajPose ?t ?p)
  )
  ;(:stream test-support-pose
  ;  :inputs (?o1 ?p1 ?o2 ?p2)
  ;  :domain (and (Pose ?o1 ?p1) (Pose ?o2 ?p2))
  ;  :certified (Supports ?o1 ?p1 ?o2 ?p2)
  ;)
  ;(:stream sample-supporting-pose
  ;  :inputs (?o1 ?p1 ?o2 ?p2)
  ;  :domain (and (Pose ?o1 ?p1) (Pose ?o2 ?p2) (Pickable ?o1) (Pickable ?o2))
  ;  :outputs (?p3)
  ;  :certified (and (Supports ?o1 ?p1 ?o2 ?p3) (Pose ?o2 ?p3))
  ;)
  ;(:stream test-pose-in-region
  ;  :inputs (?o1 ?p1 ?o2 ?p2)
  ;  :domain (and (Pose ?o1 ?p1) (Pose ?o2 ?p2))
  ;  :certified (ObjPoseInRegion ?o1 ?p1 ?o2 ?p2)
  ;)
  ;(:stream sample-supported-pose
  ;  :inputs (?o1 ?p1 ?o2 ?p2)
  ;  :domain (and (Pose ?o1 ?p1) (Pose ?o2 ?p2) (Pickable ?o2))
  ;  :outputs (?p3)
  ;  :certified (and (Supports ?o2 ?p3 ?o1 ?p1) (Pose ?o2 ?p3) (ObjPoseInRegion ?o1 ?p1 ?o2 ?p3))
    ;:inputs (?o1 ?p1 ?o2)
    ;:domain (and (Pose ?o1 ?p1) (Pickable ?o2))
    ;:outputs (?p3)
    ;:certified (and (Supports ?o2 ?p3 ?o1 ?p1) (Pose ?o2 ?p3) (ObjPoseInRegion ?o1 ?p1 ?o2 ?p3))
  ;)
  (:stream sample-supported-pose
    :inputs (?r ?o2 ?p2)
    :domain (and (Region ?r) (Pose ?o2 ?p2) (Pickable ?o2))
    :outputs (?p3)
    :certified (and (Pose ?o2 ?p3) (ObjPoseInRegion ?r ?o2 ?p3))
  )
  ;(:stream test-cfree-traj-pose
  ;  : inputs (?t ?o ?p)
  ;  : domain (and (BTraj ?t) (Pose ?o ?p))
  ;  : certified (CFreeTrajPose ?t ?o ?p)
  ;)

  (:function (MoveCost ?t)
    (and (BTraj ?t))
  )
  ;(:predicate (TrajPoseCollision ?t ?o2 ?p2)
  ;  (and (BTraj ?t) (Pose ?o2 ?p2))
  ;)
  ;(:predicate (TrajArmCollision ?t ?a ?q)
  ;  (and (BTraj ?t) (AConf ?a ?q))
  ;)
  ;(:predicate (TrajGraspCollision ?t ?a ?o ?g)
  ;  (and (BTraj ?t) (Arm ?a) (Grasp ?o ?g))
  ;)
)